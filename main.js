addEventListener(`DOMContentLoaded`,()=>{
    // #1DEFINIR SI UN NUMERO ES POSITIVO Y MENOR QUE 100.
    // #entrada de datos
    let N=Number(prompt(`ingrese el numero positivo del 1 al 100`))
    if(N<100 && N>0){
        console.log(`el numero es :${N} y esta en el rango estipulado`)
        console.log(`el numero es :${N} y esta en el rango estipulado`)
    }else{
        console.log(`el numero tiene que es tar entre el rango estipulado`)
        console.log(`el numero tiene que es tar entre el rango estipulado`)
    }

})